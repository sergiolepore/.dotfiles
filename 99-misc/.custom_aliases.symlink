alias clr="clear"
alias rst="reset"

function xdg-open-clean() {
  xdg-open "$1" &> $HOME/.xsession-errors &
}

alias ]="xdg-open-clean"
alias reload=". ~/.zshrc && echo 'ZSH config reloaded from ~/.zshrc'"
alias nodejs=node
